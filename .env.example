# ── Compute provider ─────────────────────────────────────────────────
# Which LLM backend to use for the Compute layer (Layer 3).
# Options: "openai" or "anthropic"
ACHERON_LLM_PROVIDER=anthropic

# ── OpenAI-compatible LLM settings ──────────────────────────────────
# Used when ACHERON_LLM_PROVIDER=openai (or via proxy for other APIs)
ACHERON_LLM_API_KEY=your-openai-api-key-here
ACHERON_LLM_BASE_URL=https://api.openai.com/v1
ACHERON_LLM_MODEL=gpt-4o

# ── Anthropic-native LLM settings ───────────────────────────────────
# Used when ACHERON_LLM_PROVIDER=anthropic
# Get a key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-sonnet-4-20250514
ANTHROPIC_MAX_TOKENS=4096

# ── Embeddings ───────────────────────────────────────────────────────
# Leave blank for local sentence-transformers; set for OpenAI embeddings
ACHERON_EMBEDDING_API_KEY=
ACHERON_EMBEDDING_MODEL=all-MiniLM-L6-v2

# ── NCBI / PubMed (Evidence layer) ──────────────────────────────────
# Optional — increases rate limits for PubMed API.
# Get one at: https://www.ncbi.nlm.nih.gov/account/settings/
NCBI_API_KEY=
NCBI_EMAIL=

# ── Data directories ─────────────────────────────────────────────────
ACHERON_DATA_DIR=./data
ACHERON_VECTORSTORE_DIR=./data/vectorstore

# ── Server settings ──────────────────────────────────────────────────
ACHERON_HOST=127.0.0.1
ACHERON_PORT=8000

# ── Logging ──────────────────────────────────────────────────────────
ACHERON_LOG_LEVEL=INFO
